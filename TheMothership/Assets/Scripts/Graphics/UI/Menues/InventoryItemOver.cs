using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;
/*
public class InventoryItemOver : MonoBehaviour, IPointerEnterHandler, IPointerExitHandler , IPointerClickHandler{

    private InventoryHandler inventoryHandler;
    private Item item;
    private InventoryType type;
    private MechItemClass mechClass = MechItemClass.NotMechItem;

    public void SetItem(InventoryHandler ih, Item i, InventoryType it)
    {
        inventoryHandler = ih;
        item = i;
        type = it;
    }

    public void OnPointerEnter(PointerEventData data)
    {
        inventoryHandler.OnPointerEnter(item, type, mechClass);
    }
    public void OnPointerExit(PointerEventData data)
    {
        inventoryHandler.OnPointerExit(item, type, mechClass);
    }
    public void OnPointerClick(PointerEventData data)
    {
        inventoryHandler.OnPointerClick(item,type,mechClass);
    }
}*/
