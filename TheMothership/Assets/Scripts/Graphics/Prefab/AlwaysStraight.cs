using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlwaysStraight : MonoBehaviour {

    /*public Vector3 distance = Vector3.zero;
    public Transform follow = null;

    void Update () {

        if(follow != null)
        {
            Global.removeMe.AddIfNotContains(gameObject);
        }
        else
        {
            if (transform.position != follow.position + distance)
            {
                transform.position = follow.position + distance;
            }
        }
    }*/
}
